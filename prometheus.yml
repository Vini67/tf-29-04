global:
  scrape_interval: 15s # Intervalo padrão para coleta de métricas

scrape_configs:
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres_exporter_container:9187'] # Container do Postgres Exporter